from typing import Self

from project_types import (
    BOARD_ROWS, BOARD_COLS
    GameState, Movement, PieceKind, Location, PlayerNumber, MovePossibilities, PiecePositions, LivePiece, PlayerAction, ActionType, GameStatus,
    )
from model import *

def OnlineModel(GameModel):
    ...

